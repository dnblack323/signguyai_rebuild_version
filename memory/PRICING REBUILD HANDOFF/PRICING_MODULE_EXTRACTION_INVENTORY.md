# PRICING MODULE EXTRACTION INVENTORY

**Status:** Documentation/investigation only. No code changed. This inventory covers every pricing-related module, page, route, service, model, calculator, wizard, and major component found in the legacy repository during this pass, plus prior investigation sessions (`PRICING_SYSTEM_CALCULATORS_ONBOARDING_QUIZ_REBUILD_DOC.md`, `PRICING_ISSUES_INVENTORY_AND_REMEDIATION_PLAN.md`).

**Legend for "Confidence":** `Confirmed` = read the actual code this session or a prior session and verified the claim directly. `Assumption to Verify` = plausible based on naming/structure but not read line-by-line — exact file/route to inspect is listed. `Open Business Decision` = a choice for the Owner, not a code fact.

**Dispositions used below:** Bring Over Mostly As-Is | Bring Over Formula/Business Rules Only | Refactor Before Rebuild | Merge Into Shared System | Replace Completely | Use Only as Reference | Retire / Do Not Port | Needs More Investigation

---

## A. CORE PRICING FOUNDATION

| Item | Current Purpose | Files/Routes/Models | Shared Dependencies | Current Problems | Rebuild Disposition | Bring-Over Priority | Notes |
|---|---|---|---|---|---|---|---|
| Pricing calculation dispatcher | Routes a `(category, JobItemPricingData, quantity)` call to one of 9 `calculate_*` functions | `server.py::calculate_pricing()` (line 4356), 9 `calculate_*` functions (`calculate_banners` L2126, `calculate_rigid_signs` L1698, `calculate_cut_vinyl` L867, `calculate_digital_print` L1214, `calculate_vehicle_graphics` L2683, `calculate_apparel` L3869, `calculate_services` L3237, `calculate_promotional` L696, `calculate_custom` L3739) | `models/pricing.py` (`PricingCalculation`, `JobItemPricingData`, `MaterialConfig`, `PricingDefaults`), `pricing_configuration` collection | Giant if/elif dispatcher (~3,700 lines combined); all-float math throughout; each category re-implements waste/overhead/markup/rush/warnings independently with copy-pasted logic instead of one shared engine; generic `warnings: [string]` | **Bring Over Formula/Business Rules Only** — the per-category math (coverage factors, waste tables, minimum charges, rush rules) is a valuable, largely-correct business-rule reference; the dispatcher/function structure itself is **Replace Completely** | P0 | Confirmed — read directly this session and prior session |
| `/api/pricing/calculate` endpoint | Live calculation entry point, no persistence | `routes/pricing.py` L162-186 (`calculate_price`) | `calculate_pricing()` | None specific beyond the dispatcher's own issues; this endpoint's live/no-persist behavior is actually the *correct* pattern to preserve | **Bring Over Formula/Business Rules Only** (the "live calculate, no persistence" contract is correct; internals get replaced) | P0 | Confirmed |
| Pricing Foundation settings CRUD | Get/update tenant's `PricingFoundationSettings`-equivalent defaults | `routes/pricing.py` L188-273 (`get_my_pricing_defaults`, `get_my_pricing_settings`, `update_pricing_defaults`, `update_pricing_settings`) | `pricing_configuration` collection | Settings and "defaults" are two overlapping GET endpoints reading from the same document — unclear which is authoritative (Assumption to Verify: inspect `_normalize_pricing_payload` L68 and both response shapes side-by-side) | **Refactor Before Rebuild** | P0 | Confirmed structure; the settings/defaults duplication needs the exact file read flagged above |
| Materials Library (embedded) | Per-tenant list of materials with cost/sqft, category tags | `models/pricing.py::MaterialConfig`; embedded array inside `pricing_configuration`; `routes/pricing.py::get_materials` L274-293; helper `find_material()`/`get_material_cost_per_sqft()` in `server.py` | Every area-based `calculate_*` function; `calculate_vehicle_graphics` also uses it for vehicle base-sqft lookups (L2732-2736) | Embedded array, not its own collection — no independent CRUD/audit trail per material; `compatible_categories` field exists but enforcement is inconsistent (PRC-050) | **Merge Into Shared System** — becomes the Phase 2 `pricing_materials` collection | P0 | Confirmed |
| Labor rates | Production/design/removal/install hourly rates, plus a newer per-category minute-based system | `models/pricing.py` (`LaborRateRule` and similar); `server.py::get_labor_minutes_and_rate()` (referenced L2810) | Every `calculate_*` function | Two coexisting labor systems: legacy hours-based hourly rate lookup AND a newer minute-based system with an `include_labor` toggle — both paths still live in `calculate_vehicle_graphics` (L2799-2820) | **Bring Over Formula/Business Rules Only** for the minute-based system (the intended modern path); **Retire / Do Not Port** the hours-based fallback once Phase 0's decision on it is finalized | P0 | Confirmed dual system exists; Owner decision on retiring the hours fallback is still open per Phase 3 wiring spec §3 |
| Markup rules | Category-level markup multiplier/percent applied to cost basis | Embedded in each `calculate_*` function + `cfg.get("markup_percent"/"markup_multiplier")` per category config | All calculators | Not a single shared function — each category applies markup in a slightly different order relative to pass-through costs (design/install), risking the exact ordering ambiguity Phase 0 Group 3 already had to resolve | **Bring Over Formula/Business Rules Only** | P0 | Confirmed |
| Overhead rules | Overhead % applied to a cost basis | `server.py::calculate_overhead_cost()` (L336) | All calculators call it, but pass different bases (Assumption to Verify: confirm every `calculate_*` call site passes the same `(material + production labor)` basis — Phase 0 Group 2 already flagged this as historically inconsistent) | Inconsistent basis across categories (already documented as a Phase 0 Group 2 finding) | **Bring Over Formula/Business Rules Only**, corrected to the Phase 0 Group 2 canonical basis | P0 | Confirmed function exists; per-category basis consistency needs a line-by-line pass during Phase 3-5 |
| Minimum charges | Category minimum billable price/area floor | Per-category `cfg.get("install_minimum"...)`, `min_sell` fields | All calculators | Not one shared "apply minimum" helper — repeated inline per category | **Bring Over Formula/Business Rules Only** | P1 | Confirmed pattern (e.g., `install_minimum` L2804); exact minimum-charge field name per category needs enumerating during Phase 3-5 build |
| Rush pricing | Rush surcharge applied to suggested price | Per-category rush fields (`rush_fee`, similar) | All calculators | Repeated inline logic, not shared | **Bring Over Formula/Business Rules Only** | P1 | Confirmed pattern exists; exact rush formula per category needs enumerating during Phase 3-5 build |
| Quantity discounts | Price break at quantity tiers | Referenced in Remediation Plan investigation (`PRICING_ISSUES_INVENTORY_AND_REMEDIATION_PLAN.md`) | Calculators | Not verified line-by-line this session | **Needs More Investigation** — inspect each `calculate_*` function for a quantity-tier discount block | P1 | Assumption to Verify — file: `server.py`, each `calculate_*` function |
| Hardware/accessory pricing | Add-on cost items (grommets, stakes, mounting hardware) | Referenced via `BannerSetupWizard.js` "Add-on Defaults" step (L283-383) and Rigid Signs "hardware" config | Rigid Signs, Banners calculators | Structure not verified line-by-line against `calculate_rigid_signs`/`calculate_banners` this session | **Needs More Investigation** for exact field shape; **Bring Over Formula/Business Rules Only** for the business concept | P1 | Assumption to Verify — files: `server.py` L1698-2126 (Rigid Signs, Banners) |
| Category pricing defaults | Per-category config sub-document (`category_defaults.<category>`) | `pricing_configuration.category_defaults.*`, written to by `PricingSetupQuiz.js`, `BannerSetupWizard.js`, and category-specific setup screens (`CategoryPricingMethodSetup.js`) | All calculators via `get_category_pricing_config()` | Nested dict-of-dicts with inconsistent shape across categories (PRC-004); at least two different UIs write to the same `category_defaults.banners` path (confirmed, see Onboarding row below) | **Merge Into Shared System** — becomes Phase 2 `CategoryPricingConfig` | P0 | Confirmed |
| `PricingTemplate` | Saved reusable pricing preset (e.g., "Standard 4x8 banner") | `routes/pricing.py` L294-360 (list/create/update/delete/favorite); model in `models/pricing.py` | Quotes/Order Item creation flow (Assumption to Verify: confirm exact consumption point) | Structure itself looked sound per prior investigation | **Bring Over Mostly As-Is** | P1 | Confirmed CRUD exists; consumption point in Quote/Order creation not re-verified this session |
| Pricing Setup Quiz | 11-section guided onboarding quiz converting real-world prices into Foundation defaults | `frontend/src/components/pricing/PricingSetupQuiz.js` (1,375 lines) | Writes to `category_defaults.*`, `labor.*`, `design.*` | **Confirmed overlap** with Banner Setup Wizard: both write to `category_defaults.banners` (see next row); quiz answers are session-only state, no persistence of partial progress verified | **Bring Over Formula/Business Rules Only** (the derivation formulas that turn "what do you charge for a 4x8 banner" into `sell_rate`/`min_sell` are useful reference); UI itself needs a full Phase 9 redesign as ONE unified setup flow | P1 | Confirmed — read this session (L45-52, L165, L241-244, L337-338, L486-492) |
| Banner Setup Wizard | 7-step category-specific wizard for Banners pricing setup | `frontend/src/components/pricing/BannerSetupWizard.js` (501 lines), launched from `CategoryPricingMethodSetup.js` | Writes to `category_defaults.banners` — **the exact same destination as the Pricing Setup Quiz's Banners section** | **Confirmed duplicate configuration flow.** File's own top-of-file comment says `// Saves to settings.category_defaults.banners`; steps are Pricing Method → Materials & Retail Rates → Minimum Charge → Labor Defaults → Add-on Defaults → Product Templates → Review & Save — materially overlapping with the Quiz's Banners section (benchmark prices, finishing, production minutes) | **Retire / Do Not Port** as a separate flow — **Open Business Decision**: fold its step-by-step guided UX pattern into the unified Phase 9 quiz instead of keeping two entry points that write the same data | P2 | **Confirmed** this session — read both files directly, same write destination confirmed |
| Shop Rate Quiz | Standalone shop/labor-rate-derivation calculator (home/small/growing shop presets, billable %, payroll burden %, profit buffer, rounding rule) | `frontend/src/components/pricing/ShopRateQuiz.js` (534 lines), used in `pages/PricingFoundation.js` | Feeds a derived labor rate into Foundation settings (exact write path not traced this session) | **Not confirmed redundant with the Pricing Setup Quiz** — it is a materially different, more detailed methodology (payroll burden %, billable-hours efficiency, profit buffer, rounding) than the Quiz's simpler labor questions. This corrects the prior session's "not confirmed" flag. | **Bring Over Formula/Business Rules Only** — the shop-rate-burden derivation methodology is a genuinely useful, non-duplicated business rule worth preserving as a step inside the unified Phase 9 quiz | P1 | **Confirmed distinct** this session (read L1-78 directly); exact write-destination call still needs a follow-up read |
| Historical Invoice Import | AI-assisted extraction of benchmark prices from uploaded past invoices/PDFs, with column-mapping, outlier detection, and a human-review acceptance step | `routes/pricing_setup.py` (818 lines) — `imports`, `mapping`, `analyze`, `review` endpoints; uses `ai_analyze_category_metrics`/`ai_extract_pdf_rows` | Writes accepted values into `pricing_configuration`/`category_defaults` on acceptance; the AI-suggest/human-accept pattern is architecturally correct (already flagged as the model for future AI Pricing Guidance, Phase 0/Phase 10) | None major — this module's "AI suggests, human must explicitly accept" pattern is sound and should be the template Phase 10 follows | **Bring Over Mostly As-Is** | P1 | Confirmed — read route list this session |
| Pricing snapshots (general Job Ticket) | Frozen price record attached to a Job Ticket (the platform's current generic "Order Item") | `routes/job_tickets.py` L1791-1826 (`save_ticket_pricing`) writes `pricing_snapshot = {pricing_mode, calculated_price, manual_price, active_price, calculation_breakdown, saved_at}` onto `job_tickets` | `routes/orders.py::generate_quote_from_order`/`generate_invoice_from_order` read `t.get("pricing_snapshot").get("active_price")` (L246, L359) | **This is one of at least 4 different pricing-snapshot shapes found in the repo** (see Data Ownership Map + Mistakes doc). Zero override provenance: no reason, no approver, no permission check visible on `save_ticket_pricing`. | **Bring Over Formula/Business Rules Only** — the two-step "`/calculate-pricing` live preview, then explicit `/save-pricing` commit" *workflow concept* is exactly the correct live/snapshot separation Sprint 1 formalized; the snapshot's actual shape and lack of provenance/permission gets **Replace Completely** | P0 | **Confirmed** — read `routes/job_tickets.py` L1770-1826 directly this session |
| Manual price overrides (general) | User can type a manual price instead of using the calculated one | `routes/job_tickets.py::save_ticket_pricing`, `pricing_mode: "calculator"\|"manual"` | Same as above | No reason field, no `overridden_by_user_id`, no approval workflow, no permission gate found on this endpoint | **Replace Completely** with Sprint 1's `PricingOverride` schema + `PRICING_OVERRIDE` permission | P0 | Confirmed |
| Pricing warnings/guardrails | Generic `warnings: [string]` array on calculator output | `models/pricing.py::PricingCalculation.warnings` (Assumption to Verify exact field name — inferred from `PricingCalculation` model referenced across the dispatcher) | Every `calculate_*` function | Untyped, no severity, no machine-readable warning code | **Replace Completely** with Sprint 1's typed `PricingWarning` | P0 | Confirmed generic-string pattern from prior session's direct reads; exact field name in `PricingCalculation` (L1302) should be re-confirmed during Phase 3 |
| Pricing permissions | RBAC gating on who can view/edit pricing config or override | Historically: none dedicated (confirmed absent before this sprint) | — | Zero dedicated permission existed before Sprint 1 | **Replace Completely** — now exists: `Permission.PRICING_VIEW`/`PRICING_EDIT_FOUNDATION`/`PRICING_OVERRIDE` in `models/auth.py`, Sprint 1 (2026-02-15) | P0 — **already done** | Confirmed via this job's own Sprint 1 work |
| Pricing audit/change history | Log of who changed a material cost, labor rate, markup, etc. and when | Historically: none found | — | Zero audit trail existed before Sprint 1 | **Replace Completely** — now exists: `PricingChangeLog` schema + `log_pricing_change()` helper, Sprint 1 (2026-02-15), not yet wired into any live save route | P0 — schema done, wiring is Phase 2+ | Confirmed via this job's own Sprint 1 work |

---

## B. CALCULATOR CATEGORIES

| Item | Current Purpose | Files/Routes/Models | Shared Dependencies | Current Problems | Rebuild Disposition | Bring-Over Priority | Notes |
|---|---|---|---|---|---|---|---|
| Banners | Area-based calculator: hems, grommets, pole pockets, sewing, finishing | `server.py::calculate_banners()` L2126-2683 | Area-based engine (shared with Rigid Signs/Cut Vinyl/Digital Print per Remediation Plan) | All-float math; giant function; see Foundation-level issues above | **Bring Over Formula/Business Rules Only** | P0 — Phase 3 first port | Confirmed |
| Rigid Signs | Area-based: substrates, thickness, shape, hardware, drilling | `server.py::calculate_rigid_signs()` L1698-2126 | Same area-based family | Same as Banners | **Bring Over Formula/Business Rules Only** | P0 — Phase 3, second port | Confirmed exists; substrate/hardware/drilling fields not individually verified this session |
| Cut Vinyl | Area-based: colors, weeding, transfer tape, masking | `server.py::calculate_cut_vinyl()` L867-1214 | Same area-based family | Same as Banners | **Bring Over Formula/Business Rules Only** | P0 — Phase 3 | Confirmed exists; weeding/transfer-tape fields not individually verified this session |
| Digital Print | Area-based: ink coverage, laminate, contour cutting, mounting | `server.py::calculate_digital_print()` L1214-1698 | Same area-based family | Same as Banners | **Bring Over Formula/Business Rules Only** | P0 — Phase 3 | Confirmed exists; ink-coverage/mounting fields not individually verified this session |
| Vehicle Graphics / Vehicle Wraps (Foundation engine) | Area-based extension: vehicle type, coverage %, package benchmark floor, waste-by-coverage, laminate, window perf, seam/difficulty multipliers, second installer | `server.py::calculate_vehicle_graphics()` L2683-3237 | Materials library (vehicle base-sqft lookup L2732-2736), minute-based labor system | Genuinely sophisticated, mostly sound formula — but **fully disconnected from Wrap Lab's separate, much simpler engine** (see Wrap Lab section below) | **Bring Over Formula/Business Rules Only** — becomes THE ONE canonical vehicle-wrap engine (Phase 4) | P0 — Phase 4 | **Confirmed** — read L2683-2820 directly this session |
| Apparel | Table-first: blank cost, brand/style, quantity tiers, placement, decoration method, size breakdown, plus-size, add-ons, formula fallback | `server.py::calculate_apparel()` L3869-4356 | Apparel price tables (referenced, structure not read line-by-line this session) | Not verified line-by-line this session beyond function existing | **Bring Over Formula/Business Rules Only** | P1 — Phase 5 | Assumption to Verify — file: `server.py` L3869-4356 |
| Services | Time/unit/trip-based: hourly, flat, per-unit, travel, mileage, service-call minimum, equipment, subcontracting, permit fees, overtime | `server.py::calculate_services()` L3237-3739 | None area-based; standalone module | Not verified line-by-line this session beyond function existing | **Bring Over Formula/Business Rules Only** | P1 — Phase 5 | Assumption to Verify — file: `server.py` L3237-3739 |
| Promotional | Vendor-cost/cost-plus-markup pricing | `server.py::calculate_promotional()` L696-867 | None | Not verified line-by-line this session beyond function existing | **Bring Over Formula/Business Rules Only** | P1 — Phase 5 | Assumption to Verify — file: `server.py` L696-867 |
| Custom | Manual-entry fallback for uncategorizable items | `server.py::calculate_custom()` L3739-3869 | None | Risk of becoming an escape hatch that bypasses snapshot/warning/permission discipline if not deliberately wired the same as every other category | **Bring Over Formula/Business Rules Only**, with an explicit rebuild rule that Custom still produces a `PricingResult`/`PricingSnapshot`/goes through the same permission gate | P1 — Phase 5 | Confirmed function exists; internal structure not read line-by-line this session |

---

## C. CONNECTED OPERATIONAL AREAS

| Item | Current Purpose | Files/Routes/Models | Shared Dependencies | Current Problems | Rebuild Disposition | Bring-Over Priority | Notes |
|---|---|---|---|---|---|---|---|
| Quotes | Customer-facing price proposal, own `line_items`/`subtotal` shape | `routes/quotes.py` (343 lines) — `QuoteLineItem`, subtotal calc L57-186 | Independent of `job_tickets.pricing_snapshot` — quotes appear to carry their own embedded line-item price, not a reference to a `PricingSnapshot` id (Assumption to Verify: confirm whether a Quote Item can ever be created FROM a job ticket's snapshot, vs. always being manually entered) | A third parallel "priced line item" shape, alongside `job_tickets.pricing_snapshot` and `jobs.line_items` | **Refactor Before Rebuild** — Phase 6 rewires Quote Items to consume `PricingSnapshot` directly | P1 — Phase 6 | Confirmed structure exists; snapshot-linkage question is Assumption to Verify |
| Orders / Order Items (current, called "job tickets") | The real production/pricing unit today | `routes/orders.py`, `routes/job_tickets.py`, `db.job_tickets` collection | `pricing_snapshot.active_price`, `db.material_requirements` (inventory), `db.orders` | Terminology inconsistency ("Job Ticket" vs. "Order Item," a separately tracked cleanup item); this collection is the correct modern one, but its snapshot shape is one of 4 divergent shapes (see Data Ownership Map) | **Refactor Before Rebuild** | P0 — Phase 6 | **Confirmed** — read `orders.py` L227-260, `job_tickets.py` L1770-1826 directly |
| Work Order Summary | Aggregated production ticket for a job | `routes/orders.py::generate_work_order()` L424-484 | `db.job_tickets` | Not read line-by-line this session beyond existence | **Bring Over Formula/Business Rules Only** | P2 — Phase 6 | Assumption to Verify — file: `routes/orders.py` L424-484 |
| Production time estimates | Estimated labor time per item, feeds Work Order Summary and (should) profitability reporting | Embedded on `job_tickets` items, referenced by `get_order_production_summary()` L523-567 | `job_tickets` | Not read line-by-line this session | **Needs More Investigation** | P2 — Phase 6 | Assumption to Verify |
| Inventory material usage | Reserve/pull materials against a job's material requirements | `services/inventory_service.py` (`reserve_requirement`, `reserve_order`, `allocatable_quantity`) — reads `material_requirements` collection keyed by `job_ticket_id`/`order_id` | `job_tickets`, `inventory_lots` | Genuinely well-built reservation/allocation system with lot tracking and audit trail (`log_inventory_movement` referenced) — no major problems found this session | **Bring Over Mostly As-Is** | P1 — Phase 6 | **Confirmed** — read `services/inventory_service.py` L48-263 directly this session; this module is a positive counter-example to the rest of pricing's fragmentation |
| Purchasing and vendor costs | Vendor price records feeding material cost | `models/inventory.py::VendorInput`, `PurchaseOrderInput` (per handoff's existing model references) | `pricing_materials` (future), `inventory_lots` | Not read line-by-line this session | **Needs More Investigation** for the exact vendor-cost-to-material-cost feedback loop (does a PO receipt ever update a material's `cost_per_sqft`? Not confirmed) | P2 — Phase 2/6 | Assumption to Verify — files: `models/inventory.py`, `routes/inventory.py` |
| Invoices and deposits | Billing document generated from a Quote or Order | `routes/invoices.py` (488 lines) — imports `sync_job_items_from_embedded_line_items` from `routes/jobs.py` | `jobs.line_items` (fallback), `quotes` | **Confirmed**: `invoices.py::generate_invoice_from_order`-equivalent path falls back to the legacy `jobs`/`job_items` collections (`sync_job_items_from_embedded_line_items`, itself labeled "legacy embedded" in its own docstring) rather than exclusively using `job_tickets.pricing_snapshot` | **Refactor Before Rebuild** | P0 — Phase 6 | **Confirmed** — read `invoices.py` L33, L176-206 and `routes/jobs.py` L100-125 directly |
| Profitability / margin reporting | Owner-facing margin/profit dashboards | `routes/profit_analytics.py` (587 lines) — reads `db.jobs.find()` (L167) and `db.job_items.find()` (L175), **not** `db.job_tickets` | `jobs`, `job_items`, `customers`, `sales_entries`, `expense_entries`, `invoices` | **Confirmed**: profitability reporting reads the legacy `jobs`/`job_items` collections, not the modern `job_tickets` collection that actually carries `pricing_snapshot`. This is a direct, verified instance of the "reports reading legacy/deprecated job records" mistake. | **Replace Completely** — Phase 6 rewires this to read `PricingSnapshot` records exclusively | P0 — Phase 6 | **Confirmed** — read `routes/profit_analytics.py` L167-175 directly this session |
| Tax interaction | Sales tax calculation on quotes/invoices/orders | Not located this session (no dedicated tax module found in the files inspected) | — | Unknown — could be a flat rate, per-line, or absent entirely | **Needs More Investigation** | P2 | Assumption to Verify — search `routes/quotes.py`, `routes/invoices.py`, `routes/orders.py` for `tax_rate`/`tax_amount` fields |
| Customer-specific pricing, discounts, wholesale, contract pricing | Per-customer price adjustments | `models/customer.py::Customer` class inspected directly — **no discount, wholesale, contract, or tax-exempt fields exist on the Customer model** | — | **Confirmed absent.** This is not a "current problem with a broken implementation" — it is a confirmed **missing feature entirely**. | **Needs More Investigation for Phase 7 design** (there is nothing to extract — Phase 7 designs this from scratch) | P2 — Phase 7 | **Confirmed absent** — read `models/customer.py` directly this session |

---

## D. WRAP LAB / WRAP COMMAND CENTER

| Item | Current Purpose | Files/Routes/Models | Shared Dependencies | Current Problems | Rebuild Disposition | Bring-Over Priority | Notes |
|---|---|---|---|---|---|---|---|
| Wrap project pricing engine | Independent price calculation for a wrap job: `material_labor_markup`, `per_sqft`, or `manual` method | `routes/wrap/core.py::WrapPricingConfig` (L156-172), `_compute_pricing_snapshot()` (L665-746) | **None** — this is the core problem: it does not call `server.py::calculate_vehicle_graphics()` or share any Foundation config at all | **Confirmed**: a fully independent, simpler, all-float pricing engine. No coverage %, no vehicle-type differentiation, no waste %, no materials-library lookup, no overhead concept, no permission check, no `PricingWarning`, and a fourth distinct snapshot shape (`suggested_price`/`quoted_price`/`base_cost`/`markup_amount`/`estimated_profit`/`estimated_margin_percent`) | **Replace Completely** — Phase 4's single canonical vehicle-wrap engine replaces this outright; formula is **Use Only as Reference** (its `design+production+install hours × labor_rate` structure is a useful sanity-check against the Foundation engine's more complex minute-based system) | P0 — Phase 4 | **Confirmed** — read `routes/wrap/core.py` L140-746 directly this session |
| Wrap-specific materials array | Per-ticket materials list (`material_name`, `brand`, `cost_per_sqft`, `sqft_used`) | `routes/wrap/core.py::WrapMaterialUpdate` (L142-153), `WRAP_MATERIAL_TYPES` (L175-179), stored inline on the `job_tickets` doc's `materials` array | **None** — a second, independent materials list, disconnected from the Phase 2 `pricing_materials` collection entirely | **Confirmed** duplicate materials library, scoped per-ticket instead of per-tenant | **Retire / Do Not Port** the separate list structure; **Merge Into Shared System** — Wrap Lab reads the shared `pricing_materials` collection (filtered to `vehicle_wraps`-compatible materials) exactly like every other category | P0 — Phase 4 | **Confirmed** — read directly this session |
| Vehicle tables | Vehicle type → base sqft lookup | Foundation side: embedded in the shared materials library, `category="vehicle_type"` (L2732-2736 of `server.py`); Wrap Lab side: none found — Wrap Lab does not appear to look up vehicle base sqft at all, relying entirely on manual coverage/sqft entry | Foundation's materials library | Two different sources of truth for "how big is this vehicle," one of which (Wrap Lab) may not exist as structured data at all | **Bring Over Formula/Business Rules Only** (Foundation's version) — becomes the one canonical vehicle table (Phase 2/4) | P0 — Phase 2/4 | Confirmed Foundation-side lookup exists; Wrap Lab-side absence is Assumption to Verify (needs a scan of `routes/wrap/core.py` vehicle/coverage input fields beyond what was read this session) |
| Coverage packages / package benchmark floor | "Full wrap starts at $X" benchmark price used as a floor against cost-plus pricing | Foundation side: referenced in `calculate_vehicle_graphics` docstring step 7 ("Suggested price = max(package benchmark scaled by complexity, cost-plus)"), exact benchmark table not read this session; Wrap Lab side: no benchmark concept found — Wrap Lab has no floor/ceiling sanity check at all | Foundation calculator | Wrap Lab can produce an unbounded/unsane suggested price with no benchmark guardrail | **Bring Over Formula/Business Rules Only** (Foundation's version) | P0 — Phase 4 | Foundation-side existence Confirmed via docstring; exact benchmark table structure Assumption to Verify — file: `server.py` around L2820-3237 |
| Install-time tables | Vehicle-type-specific expected install hours | Referenced in Foundation's `calculate_vehicle_graphics` labor section (L2799-2820, minute-based system); Wrap Lab side uses a flat manually-entered `install_hours` field with no table lookup (`WrapPricingConfig.install_hours`, L161) | Foundation minute-based labor system | Wrap Lab has no install-time estimation table at all — 100% manual entry every time | **Bring Over Formula/Business Rules Only** (Foundation's version, if a table exists — Assumption to Verify beyond L2820) | P0 — Phase 4 | Foundation-side minute-based system existence Confirmed; exact table shape Assumption to Verify — file: `server.py` L2820-3237 |
| Wrap quote approval | Customer approval step for a wrap price/design | `routes/wrap/core.py` — `DesignBlock.proof_status` (L196), contract/signature workflow (per handoff scope item D) | `job_tickets` (wrap-flavored) | Not read line-by-line this session beyond the `DesignBlock` model | **Bring Over Mostly As-Is** — this is workflow, not pricing engine, and is explicitly allowed to stay in Wrap Lab per the final rebuild rules (Wrap Lab may keep proof/contract/signature workflow) | P1 — Phase 4 | Confirmed model exists; full approval flow not traced this session |
| Wrap-specific snapshots | `_compute_pricing_snapshot()` output stored on the ticket | `routes/wrap/core.py` L1254-1289 (`PUT .../pricing` recomputes AND persists together — no separate live-preview-vs-commit step, unlike the general Job Ticket's `/calculate-pricing` + `/save-pricing` two-step pattern) | — | **Confirmed**: Wrap Lab's pricing PUT endpoint recalculates and saves in the same call — it does not have the live/commit separation that the general Job Ticket flow already has. This is a second, independently-confirmed reason Wrap Lab must be unified onto the shared engine. | **Replace Completely** | P0 — Phase 4 | **Confirmed** — read `routes/wrap/core.py` L1254-1289 directly this session |
| Wrap documents/packets consuming pricing | Customer packet PDF, work order PDF that display the wrap price | `routes/wrap/pdfs.py` (not read this session) | Wrap snapshot | Will need updating once the wrap snapshot shape changes to `PricingSnapshot` | **Refactor Before Rebuild** | P2 — Phase 4 | Needs More Investigation — file: `routes/wrap/pdfs.py` |
| Wrap inspection, measurements, design workflow, install workflow, aftercare, issue tracking | Non-pricing wrap-specific workflow | `routes/wrap/core.py`, `routes/wrap/portal.py`, `frontend/src/components/wrap/*`, `frontend/src/pages/WrapCommandCenterPage.js` | None pricing-related | No problems — explicitly out of scope for the pricing rebuild | **Bring Over Mostly As-Is** — these are exactly what the final rebuild rules say Wrap Lab MAY keep | P2 | Confirmed scope boundary per this task's own final rebuild rules |

---

## E. WEBSTORES

| Item | Current Purpose | Files/Routes/Models | Shared Dependencies | Current Problems | Rebuild Disposition | Bring-Over Priority | Notes |
|---|---|---|---|---|---|---|---|
| Webstore product pricing | Flat `base_cost`/`retail_price` entered manually per product | `routes/webstores.py::Product` (L173-194), `ProductCreate`/`ProductUpdate` (L197-225) | **None** — no link to `pricing_configuration`, no calculator category, no `PricingResult` | **Confirmed**: `base_cost` and `retail_price` are plain manually-typed floats with zero connection to any Pricing Foundation calculator, material, labor rate, or markup rule. Margin is whatever `retail_price - base_cost` happens to be, entered by hand. | **Replace Completely** — Phase 8 maps every product to a category calculator or Custom | P0 — Phase 8 | **Confirmed** — read `routes/webstores.py` L173-225 directly this session |
| Product-category mapping | `ProductCategory` enum used for storefront display grouping | `routes/webstores.py::ProductCategory` (L80-86): `apparel, signs, decals, promotional, events, other` | Product model | **Confirmed**: this enum does NOT map 1:1 onto the 9 Pricing Foundation calculator categories — "signs" and "decals" each collapse what should be Banners/Rigid Signs/Cut Vinyl/Digital Print into one bucket, and it has no `vehicle_wraps`/`services`/`custom` equivalents at all | **Replace Completely** — Phase 8 needs a NEW mapping field (`pricing_category`) distinct from the storefront display category | P0 — Phase 8 | **Confirmed** — read directly this session |
| Product variants | Size/color/tier options with a flat `additional_cost` | `routes/webstores.py::ProductVariant` (L162-170) | Product | Same disconnect as base product — `additional_cost` is a manually-typed float, not derived from any calculator | **Replace Completely** for the pricing linkage; **Bring Over Mostly As-Is** for the variant *concept* (size/color/tier selection UX) | P1 — Phase 8 | Confirmed |
| Public retail prices | Customer-facing price shown on the storefront | `Product.retail_price` | — | Same as above — no provenance, no link to internal cost/margin, no snapshot | **Replace Completely** — becomes the "optional public override" layer sitting on top of a real category-calculated price, per the final rebuild rules | P0 — Phase 8 | Confirmed |
| Fundraiser pricing | Event/fundraiser store profit-allocation rules | `routes/webstores.py` L321-330, L368-381, `compute_event_profit_allocation()` (L1034-1073) | `Product`/`retail_price`, `total_profit` | The profit-allocation *math* (percentage/fixed-per-item modes, running-total tracking) is sound and independent of the pricing-engine problem — it operates on whatever `total_profit` is, regardless of how that profit was calculated | **Bring Over Mostly As-Is** for the allocation logic itself; its input (`total_profit`) improves automatically once product pricing is fixed | P1 — Phase 8 | **Confirmed** — read L1034-1073 directly this session |
| Event-store pricing | Same Webstore product system, used for one-off event stores | Same `Product`/`ProductCategory` model | Same | Same disconnect as general Webstore pricing | **Replace Completely** (same fix as general Webstore products) | P1 — Phase 8 | Confirmed — same model reused |
| Owner share | Store owner's share of fundraiser profit | `LockedSettings` (L106-116, referenced fields), `profit_split` (L118) | `Product`, fundraiser profit allocation | Tenant-admin-locked fields, explicitly documented in code as "MUST NOT be overwritten by questionnaire answers... or any other non-admin flow" (L109-111) — this specific protection pattern is well-designed | **Bring Over Mostly As-Is** — the "admin-locked field, protected from other write paths" pattern is worth preserving explicitly | P1 — Phase 8 | **Confirmed** — read `LockedSettings` docstring directly this session |
| Platform fee | Emergent/platform's cut of a Webstore transaction | `webstores.py` L577 (`payment_platform_fee`, referenced on the order/checkout side) | Stripe checkout | Not read line-by-line this session beyond field existence | **Bring Over Mostly As-Is** for the fee concept; **Needs More Investigation** for the exact calculation | P1 — Phase 8 | Assumption to Verify — file: `routes/webstores.py` checkout/order-creation section |
| Discounts/promotions | Storefront-level discount codes | Not located by name in the files inspected this session | — | Unknown | **Needs More Investigation** | P2 | Assumption to Verify — search `routes/webstores.py` for `discount_code`/`promo` |
| Webstore order pricing snapshots | Frozen price record for a Webstore order line | Not confirmed to exist as a structured object this session — order creation appears to just copy `Product.retail_price` at checkout time (Assumption to Verify: read the actual checkout/order-creation function in `routes/webstores.py`, which was not reached this session due to file size — 3,776 lines) | `Product` | If no structured snapshot exists, this is a **5th** distinct "pricing at commit time" mechanism (or worse, none at all) | **Needs More Investigation**, then **Replace Completely** per the final rebuild rules (Webstore Order Item must get a real `PricingSnapshot`) | P0 — Phase 8 | Assumption to Verify — file: `routes/webstores.py`, checkout/order endpoints (not reached this session; file is 3,776 lines, only L1-260 and L1030-1080 were read) |
| Webstore profitability reporting | Store-level profit/margin dashboards | Likely reads `total_profit`/`Product.base_cost` fields directly | Same disconnected cost data as general Webstore pricing | Inherits the same disconnect — any margin report here is only as good as the manually-typed `base_cost` | **Replace Completely** (same fix as general Webstore products) | P1 — Phase 8 | Assumption to Verify — exact reporting route not located this session |
| Stripe/payment relationship | Payout, fees, and reporting tied to Webstore checkout | Referenced (`payment_platform_fee`); **the actual Stripe integration itself is out of scope for the pricing rebuild** per this task's own instructions ("only where it affects price, fees, payout, or reporting") | Webstore checkout | Not investigated beyond the fee field | **Bring Over Mostly As-Is** for the payment plumbing; only the fee *math*'s relationship to price/margin is pricing-rebuild scope | P2 | Confirmed scope boundary; Stripe integration mechanics themselves out of scope |

---

## Items explicitly flagged "Needs More Investigation" (consolidated, for the next session)

1. Whether `routes/pricing.py`'s `get_my_pricing_defaults` vs `get_my_pricing_settings` are truly two different documents or two views of one — read both handlers side-by-side.
2. Quantity-discount, hardware/accessory, minimum-charge, and rush-fee exact field shapes per category — read each `calculate_*` function in full (`server.py` L696-4356).
3. Apparel, Services, and Promotional calculators' internal logic — only their existence and line ranges were confirmed this session, not their formulas.
4. Whether a PO receipt in Purchasing ever updates a material's `cost_per_sqft` (the vendor-cost-to-material-cost feedback loop) — read `routes/inventory.py`'s PO-receive endpoint.
5. Tax calculation — not located; search `routes/quotes.py`, `routes/invoices.py`, `routes/orders.py`.
6. Whether Wrap Lab has any vehicle-table/benchmark lookup at all, or is 100% manual entry — read the rest of `routes/wrap/core.py` (only L140-750 and L1174-1330 were read; file is 2,126 lines).
7. Webstore checkout/order-creation function and whether any Webstore order pricing snapshot exists — `routes/webstores.py` is 3,776 lines; only L1-260 and L1030-1080 were read this session.
8. Discount codes/promotions on Webstores — not located by name.
9. Whether a Quote Item can ever be created directly from a `job_tickets.pricing_snapshot`, or is always manually re-entered — read `routes/quotes.py` creation flow against `routes/orders.py::generate_quote_from_order`.
10. Exact `PricingCalculation.warnings` field shape in `models/pricing.py` (L1302) — re-confirm during Phase 3.
